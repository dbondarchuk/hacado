import { ConfigurationProps } from "@hacado/builder";
import { MarketingBrowserCarouselProps } from "./schema";

export const MarketingBrowserCarouselToolbar = (
  _props: ConfigurationProps<MarketingBrowserCarouselProps>,
) => null;
